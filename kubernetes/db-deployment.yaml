apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-deployment
  namespace: llouis
  labels:
    app: db
spec:
  replicas: 1
  strategy:
    type: Recreate
  minReadySeconds: 20
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
      - envFrom:
        - secretRef:
            name: db-secret
        name: db-deployment
        image: registry.dev.afsmtddso.com/llouis-harbor-project/db:db694f3-048440d
        ports:
        - containerPort: 5432
